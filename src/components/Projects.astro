---
import { getFeaturedProjects } from '../data/projects';
const projects = getFeaturedProjects();
---
<section id="projects" class="w-full relative px-4 sm:px-8 md:px-16 pb-32 pt-20">
    <div class="text-center mb-32 project-header">
        <h2 class="text-4xl md:text-6xl font-bold tracking-tighter uppercase flex items-center justify-center gap-4">
            <span class="w-16 h-[2px] bg-white"></span>
            Featured Works
            <span class="w-16 h-[2px] bg-white"></span>
        </h2>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-16 relative max-w-7xl mx-auto w-full">
        {/* Columna Izquierda (Pares) */}
        <div class="flex flex-col gap-16 md:gap-32">
            {projects.filter((_, i) => i % 2 === 0).map((project, idx) => (
                <a href={`/projects/${project.id}`} class="project-card group block w-full no-underline cursor-none">
                    <div class="w-full aspect-[4/3] rounded-[2rem] overflow-hidden bg-[#111] relative mb-6 project-image-container" style={`background-color: ${project.backgroundColor || '#111'};`}>
                        <img src={project.images[0]} alt={project.title} class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
                    </div>
                    
                    <h3 class="text-2xl md:text-3xl font-medium tracking-tight mb-4 group-hover:text-gray-300 transition-colors">{project.title}</h3>
                    
                    <div class="flex justify-between items-center w-full">
                        <div class="flex flex-wrap gap-2">
                            {project.tags.map(tag => (
                                <span class="px-4 py-1.5 border border-white/10 rounded-full text-xs font-medium text-gray-300 bg-white/5">
                                    {tag}
                                </span>
                            ))}
                        </div>
                        <span class="text-sm text-gray-500 font-mono">{project.subtitle || '2024'}</span>
                    </div>
                </a>
            ))}
        </div>

        {/* Columna Derecha (Impares - con offset) */}
        <div class="flex flex-col gap-16 md:gap-32 md:mt-40">
            {projects.filter((_, i) => i % 2 !== 0).map((project, idx) => (
                <a href={`/projects/${project.id}`} class="project-card group block w-full no-underline cursor-none">
                    <div class="w-full aspect-[4/3] rounded-[2rem] overflow-hidden bg-[#111] relative mb-6 project-image-container" style={`background-color: ${project.backgroundColor || '#111'};`}>
                        <img src={project.images[0]} alt={project.title} class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105" />
                    </div>
                    
                    <h3 class="text-2xl md:text-3xl font-medium tracking-tight mb-4 group-hover:text-gray-300 transition-colors">{project.title}</h3>
                    
                    <div class="flex justify-between items-center w-full">
                        <div class="flex flex-wrap gap-2">
                            {project.tags.map(tag => (
                                <span class="px-4 py-1.5 border border-white/10 rounded-full text-xs font-medium text-gray-300 bg-white/5">
                                    {tag}
                                </span>
                            ))}
                        </div>
                        <span class="text-sm text-gray-500 font-mono">{project.subtitle || '2024'}</span>
                    </div>
                </a>
            ))}
        </div>
    </div>
</section>
